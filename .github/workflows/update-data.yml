name: Update data

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      
      - name: Build and run
        run: |
          cd ./sync
          go build -o sync ./main.go
          ./sync
      
      - name: Commit updated JSON
        run: |
          git config --local user.email "gcal-syncr@nocotech.org"
          git config --local user.name "gcal-syncr"
          git add -A
          git diff --staged --quiet || git commit -m "Update gcal data"
          git push
